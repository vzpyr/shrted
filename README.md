# shrted
link shortening app

# features
- shorten links to 6-character codes
- simple ui

# how to use
1. install python3
2. install requirements: `pip install -r requirements.txt`
3. set a 'secret_key' in app.py
4. start the app: `python3 app.py`
